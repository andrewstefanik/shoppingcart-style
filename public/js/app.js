
// Run this method once the window has loaded.
window.addEventListener ('load', function () {
    // $('.grid').fadeOut (2000);
    $('.body').slideUp (3000);
    // $('.grid').slideUp ('slow'), function () {
    //
    // });
});
